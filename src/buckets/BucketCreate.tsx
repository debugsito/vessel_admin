import * as React from 'react';
import { FC } from 'react';
import {
  CreateProps,
} from 'react-admin';

export const styles = {
  price: { width: '7em' },
  width: { width: '7em' },
  height: { width: '7em' },
  stock: { width: '7em' },
  widthFormGroup: { display: 'inline-block' },
  heightFormGroup: { display: 'inline-block', marginLeft: 32 },
};

const BucketCreate: FC<CreateProps> = () => (
  <></>
);

export default BucketCreate;
